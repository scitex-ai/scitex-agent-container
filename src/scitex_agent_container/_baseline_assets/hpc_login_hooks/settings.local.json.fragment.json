{
  "_comment_hpc_login_whitelist_hook": "PreToolUse Bash-matcher wiring for enforce_hpc_login_node_whitelist.sh. APPEND the single hook entry below into the EXISTING `PreToolUse` `Bash` matcher's `hooks` list in the baseline <agents_dir>/_shared/to_home/.claude/settings.json (next to deny_heavy_spartan_login.sh) -- do NOT add a second Bash matcher block. The script body PLUS its two sibling python files (hpc_login_whitelist_core.py, hpc_login_whitelist_policy.py) live next to this fragment (src/scitex_agent_container/_baseline_assets/hpc_login_hooks/) and must ALL be deployed to $HOME/.claude/hooks/pre-tool-use/ via to_home materialization -- the .sh delegates to the sibling core and FAILS OPEN (warn + allow) if the core is missing. The hook is inert off HPC login nodes: it gates on hostname vs $SAC_HPC_LOGIN_NODE_PATTERN (default spartan-login), so shipping it fleet-wide is zero-risk. Rationale + whitelist + educational-error catalogue are documented in the sibling README.md.",

  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "$HOME/.claude/hooks/pre-tool-use/enforce_hpc_login_node_whitelist.sh"
          }
        ]
      }
    ]
  }
}
