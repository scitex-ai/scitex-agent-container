{
  "_comment_heavy_job_demotion_hook": "PreToolUse Bash-matcher wiring for enforce_heavy_job_demotion.sh. APPEND the single hook entry below into the EXISTING `PreToolUse` `Bash` matcher's `hooks` list in the baseline <agents_dir>/_shared/to_home/.claude/settings.json (next to enforce_hpc_login_node_whitelist.sh) -- do NOT add a second Bash matcher block. The script body PLUS its two sibling python files (heavy_job_demotion_core.py, heavy_job_demotion_policy.py) live next to this fragment (src/scitex_agent_container/_baseline_assets/heavy_job_hooks/) and must ALL be deployed to $HOME/.claude/hooks/pre-tool-use/ via to_home materialization -- the .sh delegates to the sibling core and FAILS OPEN (warn + allow) if the core is missing. Dedicated build hosts opt out with SAC_HEAVY_JOB_GUARD_DISABLE=1; everywhere else the hook blocks known-heavy commands launched without nice/ionice and teaches the corrected command (nice -n 19 ionice -c 2 -n 7 <cmd>) plus the remote-first route. Rationale + heavy-class catalogue are documented in the sibling README.md.",

  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "$HOME/.claude/hooks/pre-tool-use/enforce_heavy_job_demotion.sh"
          }
        ]
      }
    ]
  }
}
