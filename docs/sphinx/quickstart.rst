Quickstart
==========

1. Install the package:

.. code-block:: bash

    pip install scitex-agent-container

2. Create an agent definition YAML:

.. code-block:: yaml

    name: my-agent
    role: developer
    model: claude-sonnet

3. Launch the agent:

.. code-block:: bash

    scitex-agent-container launch my-agent.yaml
