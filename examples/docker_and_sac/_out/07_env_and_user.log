── docker -e / -u examples (not run) ──
$ docker run -e FOO=bar -u "$(id -u):$(id -g)" scitex-agent-container:sdk-persistent ...

── sac equivalent (spec.yaml fragment) ──
spec:
  user: host
  env:
    FOO: bar
    API_KEY: ${API_KEY}
    HOME: ${HOME}
