── apptainer --bind examples (not run) ──
$ apptainer exec --bind "$PWD":/work sac-scitex.sif python -V
$ apptainer exec --bind "$HOME":"$HOME":ro sac-scitex.sif ...

── sac equivalent (spec.yaml fragment, v3 schema) ──
spec:
  runtime: apptainer
  apptainer:
    binds:
      - ${HOME}:${HOME}:rw
      - /scratch/${USER}:/scratch/${USER}:rw
