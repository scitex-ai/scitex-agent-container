── apptainer --env / --cleanenv examples (not run) ──
$ apptainer exec --env FOO=bar sac-scitex.sif env | grep FOO
$ apptainer exec --cleanenv --env API_KEY="$API_KEY" sac-scitex.sif ...

── sac equivalent (spec.yaml fragment, v3 schema) ──
spec:
  runtime: apptainer
  apptainer:
    env:
      FOO: bar
      API_KEY: ${API_KEY}
      HOME: ${HOME}
