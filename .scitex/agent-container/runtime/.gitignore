# Per-agent runtime state generated by `sac start` against an in-repo
# test agent (heartbeat.json, pid, session.jsonl, session_id,
# quota.json). The agent definitions in ../agents/ are tracked; only
# the live state is local-only.
*
!.gitignore
